# Build 问题台账

本文记录 normal build 相关问题。runtime preview 专题问题仍记录在 `docs/dev/runtime-preview/issues.md`。build 专题入口见 [README.md](README.md)，稳定模块说明见 [../modules/builder.md](../modules/builder.md)、[../modules/asset-db.md](../modules/asset-db.md)、[../modules/configuration.md](../modules/configuration.md)、[../modules/scripting.md](../modules/scripting.md)。

## 状态定义

| 状态 | 含义 |
| --- | --- |
| `open` | 已有可重复事实，仍需处理。 |
| `fact-gap` | 事实不足，不能用猜测推进。 |
| `deferred` | 已记录，但当前阶段不处理。 |
| `fixed` | 已有实现和验证证据。 |

## 当前问题

| ID | 问题 | 状态 | 当前结论 | 事实入口 | 处理方向 |
| --- | --- | --- | --- | --- | --- |
| BUILD-ISSUE-001 | normal build 在 `Package scripts` 阶段被 TypeScript 静态检查阻塞 | `fixed` | 已移除 `ScriptBuilder.buildBundleScript()` 中对 `runStaticCompileCheck()` 的 hard fail 调用。复跑 normal build 后，不再出现 Tiled tileset XML `.tsx` 被 TypeScript 当 TSX 源码解析的 `TS1109`、`TS1005` 阻塞；构建已进入真正的 `buildScriptCommand` worker。 | [../build-extension-hooks-20260612.md](../build-extension-hooks-20260612.md) | 不手工修改 `temp/cli/tsconfig.cocos.json`。后续若要恢复静态检查，必须改为 asset-aware 或 report-only，不能再用 broad `assets/**/*` 作为 build hard gate。 |
| BUILD-ISSUE-002 | normal build 在真正脚本打包阶段无法解析 `db://localization-editor/l10n` | `fixed` | 去掉静态检查后曾失败于 `db://localization-editor/l10n`。用户安装 `localization-editor` 后，CLI build 已能通过 extension AssetDB mount 解析该 domain，`debug.log` 中可见其解析到 `extensions/localization-editor/static/assets/l10n.ts`。之后主测试项目已在 `5f735ccb chore: remove localization and auto test assets` 移除 `localization-editor` 相关资源，该 domain 不再参与当前 `web-mobile` build。 | [../build-extension-hooks-20260612.md](../build-extension-hooks-20260612.md) | 保留为历史事实。若后续重新引入 project extension asset-db mount，再按 Editor domain 注册行为复验；当前不作为 build blocker。 |
| BUILD-ISSUE-003 | normal build 无法解析 `db://automation-framework/runtime/test-framework.mjs` | `fixed` | 该问题来自历史测试项目中的 `assets/auto-test-case/dynamic/2D/2d-rendering-in-3d.test.ts` import `db://automation-framework/runtime/test-framework.mjs`。主测试项目已在 `5f735ccb chore: remove localization and auto test assets` 删除 `auto-test` / `automation-framework` 相关资源；当前 `git ls-files` 和 `git grep` 在 `assets`、`extensions`、`profiles` 下已无 `automation-framework` / `auto-test-case` / `test-framework` 引用，最新 `web-mobile` 构建也已通过。 | [../build-extension-hooks-20260612.md](../build-extension-hooks-20260612.md)；[facts/meta-library-editor-parity-20260613.md](facts/meta-library-editor-parity-20260613.md) | 当前不实现 `automation-framework` domain 支持。若未来重新引入 automation framework 资源，应作为新的 Editor package/domain 加载问题登记，而不是复用本条历史 blocker。 |
| BUILD-ISSUE-004 | `--build-config` 不能等价使用 Creator profile `profiles/v2/packages/web-mobile.json` | `fixed` | 已新增 Creator profile adapter。`web-mobile.json` 中多数 `builder.common` 字段与 CLI common build options 同名且语义基本对得上；`builder.taskOptionsMap.<taskId>` 中的 `useWebGPU`、`orientation`、`embedWebDebugger` 与 `packages["web-mobile"]` 的叶子字段同名且语义对得上。当前实现会 flatten `builder.common`，将选中的 task options 写入 `packages[platform]`，并保留原有 `pluginManager.checkOptions()` 校验。最新复跑 `node .\dist\cli.js build --project ... --platform web-mobile --build-config profiles\v2\packages\web-mobile.json` 退出码为 `0`。 | [../build-extension-hooks-20260612.md](../build-extension-hooks-20260612.md) | 多 task profile 需要显式 `--taskId`；该修复只覆盖本次确认的 Creator build profile adapter，不表示所有 Creator profile package schema 都已完整兼容。 |
| BUILD-ISSUE-005 | 普通 build 测试 fixture 在浏览器启动期执行 unresolved CommonJS bare specifier | `fixed` | 修复前，`assets/cases/scripting/commonjs-bare-specifier/commonjs-bare-specifier.js` 将 `require('@tbmp/mp-cloud-sdk')` 放在模块顶层，web 启动加载该 CJS facade 时立即执行，浏览器运行时报 `Current environment does not provide a require()`。后续对比真实 `tdanalytics.mg.cocoscreator.min.js` 后确认该 fixture 不准确：真实文件把该 `require()` 放在 platform proxy constructor 内，属于延迟执行分支。当前 fixture 已修正为延迟执行形态；曾尝试的空 CJS module fallback 已回退，普通 build 和 runtime preview fallback 仍只生成 `export const __cjsMetaURL = '<specifier>';`，用于编译/打包继续和诊断。 | [../build-extension-hooks-20260612.md](../build-extension-hooks-20260612.md) | CLI 不提供 `@tbmp/mp-cloud-sdk` 的 runtime mock。若业务实际执行该平台 SDK 分支，仍需通过 import map、真实依赖或平台注入解决；运行时报缺失 host `require()` 是合理暴露，不应被空对象掩盖。 |
| BUILD-ISSUE-006 | CLI 生成的 `cocos.config.json` 可能滞后于 Editor 修改后的项目配置 | `fixed` | 已将 `cocos.config.json` 收敛为 CLI-owned overlay；Editor-owned 配置每次从 Editor settings/profiles 读取并在 runtime 内存合并。保存时只写 CLI-owned import 字段和 metadata，旧 Editor-owned 快照会被忽略并在下一次 save 清理。 | [../../superpowers/specs/2026-06-13-cocos-config-editor-owned-runtime-merge-design.md](../../superpowers/specs/2026-06-13-cocos-config-editor-owned-runtime-merge-design.md)；[../../superpowers/plans/2026-06-13-cocos-config-editor-owned-runtime-merge.md](../../superpowers/plans/2026-06-13-cocos-config-editor-owned-runtime-merge.md)；`src/core/configuration/script/manager.ts`；`src/core/configuration/script/owner-map.ts` | 保持 owner map、manager reload/remigrate、真实 Editor files 和 AssetDB config sync 测试覆盖。 |
| BUILD-ISSUE-007 | CLI build/AssetDB 改写项目级 `library` 顶层 `internal` JSON，产物 schema/version 与 Editor baseline 不一致 | `open` | 已用 local `@cocos/asset-db` 将 `internal` DB 的 `.internal-data.json`、`.internal-dependency.json` 和 `.internal-info1.0.0.json` 写入路径/schema 收敛到 Editor baseline；移除旧污染 `.internal-info.json` 后复验，`.internal-data.json` 与 `.internal-dependency.json` build 后 raw/canonical hash 仍与 baseline 一致。剩余差异是 `.internal-info1.0.0.json` 中 1 个 engine asset `D:\workspace\engines\cocos\3.8.6\editor\assets\primitives.fbx.meta` 的 `time` 字段随当前 engine source mtime 写回，导致 hash 不一致。 | [facts/meta-library-editor-parity-20260613.md](facts/meta-library-editor-parity-20260613.md)；[../../superpowers/plans/2026-06-13-asset-db-custom-source.md](../../superpowers/plans/2026-06-13-asset-db-custom-source.md)；`packages/asset-db/src/libs/info.ts`；`packages/asset-db/src/libs/dependency.ts`；`packages/asset-db/src/libs/data.ts`；`packages/asset-db/libs/asset-db.js` | 继续核对 Editor 在相同 engine source mtime 下是否也会更新 `.internal-info1.0.0.json` 的 `time`。若 Editor 不写该差异，CLI 需要让 `internal` readonly DB 的 info record 保持 Editor baseline；不能用 build 后回滚掩盖。 |
| BUILD-ISSUE-008 | CLI build 期间非 3D `typescript` `.meta` 被改为 `imported: false` | `fixed` | 原复现来自移除 `localization-editor` 前的旧构建污染：日志显示 TypeScript importer 失败根因是 `localization-editor/static/assets/l10n.ts` 的 `./core/l10n-manager` 在 AssetDB 启动期增量编译阶段解析失败，随后 `.meta.imported` 被写为 `false`。当前测试项目已移除 `extensions/localization-editor`；恢复 3 个 `.ts.meta` baseline 后重新执行当前 CLI build 和 `web-mobile` 构建，3 个 `.ts.meta` 均未再产生 diff，最新构建日志也未再出现 `l10n-manager`、`Failed to import script` 或 `resolve_error_module_not_found`。 | [facts/meta-library-editor-parity-20260613.md](facts/meta-library-editor-parity-20260613.md)；`src/core/assets/asset-handler/assets/typescript.ts`；`src/core/assets/asset-handler/assets/javascript.ts`；`src/core/scripting/packer-driver/index.ts` | 当前状态不再复现，保持事实记录。若后续重新引入 extension asset-db 脚本 mount 并复现同类失败，应作为新的 extension script importer/module graph 问题处理，不能通过强行恢复 `imported: true` 或禁止 `.meta` 写回解决。 |
| BUILD-ISSUE-009 | CLI build 改写 3D source `.gltf/.glb/.fbx.meta`，与 Editor baseline 不一致 | `fixed` | 已对齐 Editor 3.8.6 baseline：`gltf/fbx` 顶层 importer `ver` 使用 `2.3.13`，`gltf-animation` subMeta importer `ver` 使用 `1.0.17`；3D original animation 和 `gltf-animation` library files 保存为 `.cconb`，不再写成 `.bin`。新增 `vitests/suites/build/3d-source-meta-editor-alignment.test.ts` 对 `.gltf/.glb/.fbx.meta` 做 Editor baseline parity，验证中递归忽略已确认允许改写的 `userData`，仍强校验 `ver`、`files` 和 subMeta 结构。 | [facts/meta-library-editor-parity-20260613.md](facts/meta-library-editor-parity-20260613.md)；`src/core/assets/asset-handler/assets/gltf.ts`；`src/core/assets/asset-handler/assets/fbx.ts`；`src/core/assets/asset-handler/assets/gltf/animation.ts`；`vitests/suites/build/3d-source-meta-editor-alignment.test.ts` | 保持 3D source meta parity 测试覆盖；后续若出现非 `userData` 字段 diff，登记新问题或重新打开。 |
| BUILD-ISSUE-010 | CLI build 产物运行时对 `.anim` CCON 资源请求 `import/*.json` 并 404 | `fixed` | 对比 Editor 产物和 CLI 产物确认，失败资源为 `Easing_Linear.anim`、`Easing_Bounce.anim`、`Easing_Sine.anim` 三个 UUID。Editor 产物将其输出为 `assets/main/import/*.cconb` 且 `extensionMap[".cconb"]` 包含对应 UUID；修复前 CLI 因 `hasCCONFormatAssetInLibrary()` 仍只识别 `meta.files === [".bin"]`，把当前 `.anim.meta` 的 `files: [".cconb"]` 当普通 native 文件输出到 `assets/main/native/*.cconb`，config 缺少 `extensionMap`，运行时按默认 `.json` 请求导致 404。已让 CCON 识别支持 `.bin` 和 `.cconb`，并让 build 输出统一写 `.cconb`。 | [../build-extension-hooks-20260612.md](../build-extension-hooks-20260612.md)；`src/core/builder/worker/builder/utils/cconb.ts`；`src/core/builder/worker/builder/manager/asset-library.ts`；`vitests/suites/build/cconb-library-detection.test.ts` | 保持 `.cconb` library detection 单测；后续若其它资源类型仍出现 `import/*.json` 404，应按失败 UUID 继续比较 `extensionMap`、`versions.import/native` 和实际输出目录，不要用运行时兼容 `.json` 掩盖。 |
| BUILD-ISSUE-011 | 直接把 Creator platform profile 当作 parity 构建配置会丢失完整 task options | `deferred` | 对比当前主测试项目 `web-mobile` Editor 产物与旧 CLI profile-wrapper 产物，旧 CLI 多约 `6.222 MiB`，差异主要集中在 `cocos-js`：旧 CLI 输出 `settings.engine.debug=true`、main `config.debug=true`，Editor baseline 为 `false`。追加验证显示，改用 Editor 导出的完整构建配置 `buildConfig_web-mobile.json` 后，CLI 输出 `debug=false`、engine `compress:true`，产物约 `194.409 MiB`，比旧 profile-wrapper 产物小 `8.220 MiB`，也不再大于当前 Editor baseline。当前阶段确认 parity 输入应使用 Editor 导出的完整 `buildConfig_<platform>.json`，例如 `buildConfig_web-mobile.json`、`buildConfig_wechatgame.json`，不应把 `profiles/v2/packages/<platform>.json` 作为标准构建配置。 | [facts/build-output-size-editor-parity-20260615.md](facts/build-output-size-editor-parity-20260615.md)；`src/commands/build-config.ts`；`src/core/builder/share/builder-config.ts`；`src/core/builder/worker/builder/asset-handler/script/engine.ts`；`src/core/builder/worker/builder/asset-handler/script/build-script.ts` | 保留 profile adapter 作为历史兼容/诊断能力，但不把“合并 `profiles/v2/packages/builder.json` task options”作为当前 parity 必需修复。后续若明确要求直接消费 Creator profile wrapper，再重新打开并设计完整 profile graph 合并。 |
| BUILD-ISSUE-012 | `wechatgame` 高级 Editor options 尚未映射到 CLI production 行为 | `deferred` | 第一阶段只支持普通微信小游戏构建所需的 `appid`、`orientation`、`highPerformanceMode`。Creator Editor/dump 中可见但当前未支持的字段包括 `separateEngine`、`localSeparateEngine`、`wasmSubpackage`、`buildOpenDataContextTemplate`，这些字段涉及微信 engine plugin、WASM 分包和开放数据域模板，不能只按字段名暴露为 CLI production 行为。 | [facts/wechatgame-source-inventory-20260615.md](facts/wechatgame-source-inventory-20260615.md)；[../../superpowers/plans/2026-06-15-wechatgame-platform-implementation.md](../../superpowers/plans/2026-06-15-wechatgame-platform-implementation.md)；`src/core/builder/platforms/wechatgame/config.ts`；`src/core/builder/platforms/wechatgame/hooks.ts` | 第二阶段逐项补事实和验证。优先级建议：`separateEngine` 单独设计并复用 CLI 现有 split engine 管线；`buildOpenDataContextTemplate` 需先确认 Editor baseline 产物；`localSeparateEngine`、`wasmSubpackage` 暂不默认暴露。 |
| BUILD-ISSUE-013 | `wechatgame` 自动打开微信开发者工具的 `run` stage 尚未实现 | `deferred` | 当前 `wechatgame` 构建产物可被微信开发者工具打开；用户更新微信开发者工具后，人工运行测试未再复现先前 `loadFont:fail no permission` 问题。CLI 已验证本机 `cli.bat open --project <dir> --port <ide-port> --lang zh` 可打开项目，但平台 `config.customBuildStages` 当前未注册 `run`，也没有实现 `wechatDevtools` 路径来源、跨平台 CLI 路径解析和错误提示。 | [facts/wechatgame-source-inventory-20260615.md](facts/wechatgame-source-inventory-20260615.md)；`src/core/builder/platforms/wechatgame/config.ts`；`src/core/builder/manager/plugin.ts` | 作为便利功能单独实现，不阻塞第一阶段“产物可手工打开运行”。实现前需确定路径来源：构建配置、环境变量、命令行参数或 CLI 配置文件；不能依赖 Editor `Profile`。 |
| BUILD-ISSUE-014 | 项目自定义 builder hook / build extension hook 尚未接入 normal build | `open` | CLI builder 已有 hook 执行机制，`PluginManager` 支持已注册 platform package 的 `hooks` 和 `customBuildStages`；但当前扫描范围是 CLI 内置平台目录和 workspace `packages/platforms`，未发现 production 代码扫描 `<projectRoot>/extensions/*` 或项目包作为 builder hook 来源。现有项目 extension 扫描主要服务 AssetDB mount，不会把 `extensions/*` 的 builder hook 注册到 `PluginManager`。因此项目自定义 hook 目前不能视为已支持。 | [../build-extension-hooks-20260612.md](../build-extension-hooks-20260612.md)；`src/core/builder/manager/plugin.ts`；`src/core/assets/manager/plugin.ts` | 需要单独设计项目 extension builder hook 接入：扫描范围、`package.json` 字段兼容、hook 顺序、public/internal 调用签名、错误传播、与 runtime preview 的边界和测试覆盖。拿到实际项目 hook 后，先判断是否依赖 `Editor.*` 或 Creator extension 生命周期。 |

## 记录规则

- 事实证据应写入对应 `facts/` 或专题事实文档，再从本台账引用。
- 不能把测试缓存、临时配置或手工修改 `temp` 文件作为 production 默认策略依据。
- 修改方案必须说明是否会阻塞 normal build，以及与 Editor / AssetDB / `@cocos/lib-programming` 行为的关系。
