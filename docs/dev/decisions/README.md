# Architecture Decisions

本目录保存长期设计决策。只有存在明确取舍、替代方案和后果的内容才进入 `decisions/`。

普通事实进入 `../facts/`。当前模块行为进入 `../modules/`。Superpowers spec 进入 `../../superpowers/specs/`，implementation plan 进入 `../../superpowers/plans/`。
